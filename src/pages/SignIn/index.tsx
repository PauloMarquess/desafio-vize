import Input from '../../components/Input';
import { Container } from '../../styles/global';

const SignIn = () => {
  return (
    <Container>
      <Input />
    </Container>
  );
};

export default SignIn;
